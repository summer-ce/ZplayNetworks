#if UNITY_ANDROID
public enum AndroidPermission
{
    WRITE_EXTERNAL_STORAGE, //Shouldn't be used at all
    ACCESS_COARSE_LOCATION, //Shouldn't be used at all
    READ_PHONE_STATE, //Shouldn't be used at all
}
#endif